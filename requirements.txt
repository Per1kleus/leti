# Core
httpx>=0.27.0
pyyaml>=6.0.1

# Memory
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Voice pipeline
openwakeword>=0.6.0
openai-whisper>=20231117
pyaudio>=0.2.14
numpy>=1.26.4
pyttsx3>=2.90
# pyttsx3 uses your OS's native TTS engine. On Linux it needs espeak installed:
#   sudo apt install espeak

# Vision
mss>=9.0.1
Pillow>=10.4.0

# OS / desktop control
pyautogui>=0.9.54
pygetwindow>=0.0.9

# Browser automation
playwright>=1.45.0

# Web search
ddgs>=6.0.0

# Cybersecurity tools (port/process/connection inspection)
psutil>=6.0.0

# Email and trading tools use only the stdlib (imaplib/smtplib/email) and
# httpx (already listed under Core) - no additional packages needed.

# Meeting scheduling: CalDAV calendar client (Zoom/Teams use plain httpx, already listed).
caldav>=1.3.9

# GUI mode (--mode gui) - the double-click launchers run this mode by default now,
# so both packages below are real (non-optional) dependencies, not opt-in extras.
# aiohttp serves the interface over HTTP+WebSocket to any browser, including a
# phone's - required for GUI mode to work at all. pywebview wraps that same
# server in a native app window instead of a browser tab; on Linux it also needs
# a system package the launchers attempt to install automatically (best-effort -
# see scripts/_run_gui_mode.sh) - without it, GUI mode still works, just as a
# browser tab you open yourself instead of an automatic window.
aiohttp>=3.9
pywebview>=5.0
